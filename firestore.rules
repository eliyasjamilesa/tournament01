
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development Mode Rules: Allows any authenticated user to read and write
    // This solves the 'Production Mode' restriction issues during development
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
